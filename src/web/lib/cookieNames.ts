// The two cookies the web app keeps. Set by the login route handler, read by every page, deleted by the logout route handler.
export const LoginTokenCookieName = "pos_token";

export const CurrentUserCookieName = "pos_user";
